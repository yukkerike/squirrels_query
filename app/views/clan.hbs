<!DOCTYPE html>
<html lang="ru" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta property="og:type" content="profile">
    <meta property="og:title" content="{{ info.name }} (ID {{ id }})">
    <meta property="og:description" content="Уровень {{ rank.level }} • Лидер {{ leader_id.name }}">
    <meta property="og:image" content="{{ info.photo }}">
    {{> styles}}
    <title>{{ info.name }} — клан</title>
</head>
<body>
    <div class="ct">
        {{> navbar}}
        <div class="grid">
            <div class="panel">
                <h2>Клан</h2>
                <div class="badges">
                    {{#if ban}}<span class="badge badge-ban">Бан</span>{{/if}}
                </div>
                <ul class="meta-list">
                    <li class="meta-item"><span class="label">ID</span><span>{{ id }}</span></li>
                    <li class="meta-item"><span class="label">Название</span><span>{{ info.name }}</span></li>
                    <li class="meta-item"><span class="label">Уровень</span><span>{{ rank.level }}</span></li>
                    <li class="meta-item"><span class="label">Лимит уровня</span><span>{{ level_limiter }}</span></li>
                    <li class="meta-item"><span class="label">Опыт</span><span>{{ rank.exp }}</span></li>
                    <li class="meta-item"><span class="label">Рейтинг за период</span><span>{{ rating_score }}</span></li>
                    <li class="meta-item"><span class="label">Лидер</span><span><a href="/user/{{ leader_id.uid }}" target="_blank" rel="noopener">{{ leader_id.name }}[{{ leader_id.level }}]</a></span></li>
                </ul>
                <div style="margin-top:1rem;">
                    <p class="section-title" style="margin-bottom:.3rem;">Эмблемы</p>
                    <div class="emblems">
                        <img src="{{ info.emblem }}" alt="emblem" />
                        <img src="{{ info.photo }}" alt="photo" />
                    </div>
                </div>
                <div style="margin-top:1rem;">
                    <p class="section-title" style="margin-bottom:.3rem;">Новость</p>
                    <pre style="white-space:pre-wrap; overflow-wrap:anywhere; font-size:.75rem; margin:0; line-height:1.3;">{{ news }}</pre>
                </div>
            </div>
            <div class="panel">
                <h2>Статистика суток</h2>
                <div class="stat">
                    <table>
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Игрок</th>
                                <th>Игрок EXP</th>
                                <th>Клан EXP</th>
                                <th>Рейтинг</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each statistics}}
                            <tr>
                                <td>{{ inc @index}}</td>
                                <td><a href="/user/{{ this.uid.uid }}" target="_blank" rel="noopener">{{ this.uid.name }}</a></td>
                                <td>{{ this.samples }}</td>
                                <td>{{ this.exp }}</td>
                                <td>{{ this.clan_rating }}</td>
                            </tr>
                            {{/each}}
                            <tr>
                                <td></td>
                                <td><strong>Всего</strong></td>
                                <td>{{ rank.dailyPlayerExp }}</td>
                                <td>{{ rank.dailyTotalExp }}</td>
                                <td>{{ rank.DailyTotalRaiting }}</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="panel">
                <h2>Члены ({{ count.members }})</h2>
                <div class="chips">
                    {{#each members}}
                    <a href="/user/{{ this.uid }}" class="chip" target="_blank" rel="noopener"><span>{{ this.name }}</span><span>[{{ this.level }}]</span></a>
                    {{/each}}
                </div>
            </div>
            {{#if count.blacklist}}
            <div class="panel">
                <h2>Чёрный список ({{ count.blacklist }})</h2>
                <div class="chips">
                    {{#each blacklist}}
                    <a href="/user/{{ this.uid }}" class="chip" target="_blank" rel="noopener"><span>{{ this.name }}</span><span>[{{ this.level }}]</span></a>
                    {{/each}}
                </div>
            </div>
            {{/if}}
        </div>
    </div>
</body>
</html>