<!DOCTYPE html>
<html lang="ru" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta property="og:type" content="profile">
    <meta property="og:title" content="{{ name }} [{{ level }}] (UID {{ uid }})">
    {{#if profile}}<meta property="og:description" content="{{ profile }}">{{/if}}
    {{> styles}}
    <title>{{ name }} — профиль</title>
</head>
<body>
    <div class="ct">
        {{> navbar}}
        <div class="grid">
            <div class="panel">
                <h2>Игрок</h2>
                <div class="nickname">{{ name }} <span style="opacity:.5; font-weight:500;">[{{ level }}]</span></div>
                <div class="badge-row">
                    {{#if moderator}}<span class="badge badge-mod">Модератор</span>{{else}}{{#if online}}<span class="badge badge-online">Онлайн</span>{{else}}<span class="badge badge-offline">Оффлайн</span>{{/if}}{{/if}}
                    {{#if vip_exist}}<span class="badge badge-vip">VIP</span>{{/if}}
                    <span class="badge">{{ sex }}</span>
                </div>
                <ul class="meta-list">
                    <li class="meta-item"><span class="label">UID</span><span>{{ uid }}</span></li>
                    <li class="meta-item"><span class="label">Клан</span><span>{{#if clan_id}}<a href="/clan/{{ clan_id }}" target="_blank" rel="noopener">{{ clan_id }}</a>{{else}}—{{/if}}</span></li>
                    <li class="meta-item"><span class="label">Страница</span><span>{{#if profile}}<a href="{{ profile }}" target="_blank" rel="noopener">{{ profile }}</a>{{else}}—{{/if}}</span></li>
                    <li class="meta-item"><span class="label">Дата рождения</span><span>{{ bdate }}</span></li>
                </ul>
            </div>
            <div class="panel">
                <h2>Прогресс</h2>
                <div class="progress-block">
                    <div class="progress-row"><span>Уровень</span><span>{{ level }} / 200</span></div>
                    <div class="progress" style="--value: {{ level_progress }}%"><span></span></div>
                    <div class="progress-row"><span>Шаман</span><span>{{ shaman_level }} / 51</span></div>
                    <div class="progress" style="--value: {{ shaman_level_progress }}%"><span></span></div>
                </div>
                <div class="stats-grid">
                    <div class="stat-tile"><p class="stat-label">Опыт</p><div class="stat-value">{{ exp }}</div></div>
                    <div class="stat-tile"><p class="stat-label">Опыт шамана</p><div class="stat-value">{{ shaman_exp }}</div></div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>